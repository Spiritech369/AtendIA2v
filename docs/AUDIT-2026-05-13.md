# AtendIA v2 — Auditoría completa

**Fecha:** 2026-05-13
**Branch auditado:** `main` @ `9adda34`
**Worktree:** `.claude/worktrees/beautiful-mirzakhani-55368f`

Recorrido sistemático de los 14 ítems del sidebar + flujos transversales,
identificando qué luce real pero no lo es, qué se queda a medias, y qué
nunca llegó a cablearse. Cada hallazgo tiene `file:line` y una sugerencia
concreta. Prioridad: **🔴 alta** (sorprende al usuario), **🟡 media** (deuda
visible), **🟢 baja** (cosmético).

---

## TL;DR — Top 10 a arreglar primero

1. 🔴 **Workflow `simulate` devuelve resultados fabricados** (no ejecuta nodos reales). `core/atendia/api/workflows_routes.py:1271-1311` — los operadores no pueden validar lógica antes de activar.
2. 🔴 **Workflow triggers viven en cron de 10s** sin hook inline en el runner — inbound entre polls puede no disparar el workflow. `core/atendia/queue/workflow_jobs.py:38-98`.
3. 🔴 **Edición de pipeline (stages/transitions) no recarga conversaciones activas** — el runner cachea `load_active_pipeline` al boot. Cambios sólo aplican a turnos nuevos tras reinicio.
4. 🔴 **Knowledge Base command center es ~70% hardcoded** (health, history, risks, items, unanswered, funnel-coverage, audit-logs) en `_kb/command_center.py:704-901`. Lo que ves no son tus datos.
5. 🔴 **`POST /knowledge/simulate` devuelve 501 para tenants no demo** — feature anunciada pero inaccesible. `_kb/command_center.py:806-826`.
6. 🔴 **Handoffs command center inyecta 20 handoffs demo hardcoded** (`SEED_HANDOFFS`) para tenants demo, mezclados con los reales. `_handoffs/command_center.py:554-561`. Draft-reply siempre marca `source="mock"`.
7. 🟡 **Workflow pause/save-view/import-export** — 6 botones `NYIButton` en `WorkflowsPage.tsx:524-680` no hacen nada al click.
8. 🟡 **Appointments importar/detail/filtros** — 5 botones NYI + 2 `DemoBadge` en `AppointmentsPage.tsx:392-846`.
9. 🟡 **`exports_routes.py` no tiene tests** — las 2 rutas CSV podrían romperse sin que nadie note.
10. 🟡 **Asignación de Agente a conversación es metadata-only** — `assigned_agent_id` se guarda pero el runner no lo lee. `conversations_routes.py:941-949`.

---

## Por módulo del sidebar

### 1. Dashboard
| Estado | Notas |
|---|---|
| ✅ Funciona | `/api/v1/dashboard/summary` calcula counts reales (customers, conversaciones today/active/unanswered, citas, appointments_today). Render OK. |
| 🟡 Gap | No hay tests E2E del dashboard. Componente `DashboardPage.tsx` sin `.test.tsx`. |

**Sugerencias:**
- Si los KPIs te muestran 0 con el usuario nuevo (`dele.zored`), es esperado — el tenant arranca vacío.
- Agregar `DashboardPage.test.tsx` con mocks de `dashboardApi.getSummary`.

---

### 2. Conversaciones (Inbox)

| Flujo | Estado |
|---|---|
| Listado conversaciones | ✅ funciona |
| Detalle + mensajes | ✅ funciona |
| Mark-read / unread badge | ✅ funciona |
| Intervene (operador toma control) + send | ✅ persiste outbound, marca bot_paused, enqueue Meta |
| Resume bot | ✅ funciona — runner respeta `bot_paused` y short-circuita |
| Eliminar conversación | ✅ funciona (soft delete) |
| Realtime (WebSocket) | ✅ funciona en happy path |
| **Contact panel — DATOS DE CONTACTO editables** | ✅ funciona (sprint anterior) |
| **AI Field Extraction → sugerencias** | ✅ funciona (sprint anterior) |

**Sugerencias:**
- `MessageBubble.tsx`, `ChatInput`, `ConversationSearch` no tienen tests. Agregar al menos render-tests.
- El botón "Abrir WhatsApp" en `ContactPanel.tsx:495` es `NYIButton` — fácil hookear con `wa.me/<phone>` deep link. **Es 1 línea**.

---

### 3. Handoffs (Escalaciones)

| Estado | Notas |
|---|---|
| 🟡 Mezcla real + mock | `command_center.py:554-561` inyecta `SEED_HANDOFFS` (20 cases hardcoded) para tenants demo. Para tu tenant nuevo NO se inyectan, así que verás 0 hasta que escale algo real. |
| 🟡 Draft-reply | Siempre devuelve `source="mock"`. Hay `DemoBadge` en `HandoffCard.tsx:41-50`. |
| ✅ Take / Resolve / Feedback | Funcionan contra la tabla real `human_handoffs`. |
| ✅ SLA + risk + payload | Reales (escritos por el runner cuando escala). |

**Sugerencias:**
- Implementar draft-reply real conectando al composer del runner.
- `HandoffQueue.tsx` y `HandoffCard.tsx` sin tests.

---

### 4. Clientes (CRM)

| Flujo | Estado |
|---|---|
| Listado + búsqueda + filtros | ✅ funciona |
| Detalle (score, risks, NBA, timeline, docs, messages) | ✅ funciona |
| Import CSV (preview + commit) | ✅ funciona — dedup en file + DB, normaliza phone, source="CSV", errores por fila |
| Export CSV | ✅ funciona (`exports_routes.py`) **pero sin tests** |
| Notas (CRUD + pin) | ✅ funciona |
| Custom field definitions (admin) + values (operator) | ✅ funciona |
| **Auto-seed demo data** | ⚠️ `_ensure_demo_data` en `customers_routes.py:1162-1240` crea 50 customers automáticamente cuando el tenant es demo. **Para `dele.zored` NO aplica** (`is_demo=false`). |

**Sugerencias:**
- Agregar tests a `exports_routes.py` (mínimo: dos rutas devuelven 200 + content-type).
- `ClientsPage.tsx`, `CustomerDetail.tsx`, `CustomerSearch.tsx`, `ImportCustomersDialog.tsx` sin tests.

---

### 5. Pipeline (Kanban)

| Estado | Notas |
|---|---|
| ✅ Board + drag-to-stage | Funciona |
| 🔴 **Editar pipeline no recarga conversaciones activas** | El runner carga `load_active_pipeline` una sola vez. Hasta que la conversación pase por otro turno, sigue con el pipeline viejo. Riesgo: ver pipeline cambiado pero bot sigue ejecutando el anterior. |
| 🟡 "Cargar más" no cableado | Documentado en el runbook, no en código. |

**Sugerencias:**
- Invalidar el cache del pipeline en cada turno (cheap: leer versión cada vez), o emitir un evento Redis al actualizar pipeline y que el runner lo capture.
- `PipelineKanbanPage.tsx` sin tests.

---

### 6. Citas (Appointments)

| Flujo | Estado |
|---|---|
| Crear cita manual | ✅ persiste con conflict detection + risk recalculation |
| KPIs, agenda, vistas | ✅ funciona |
| Parser natural ("mañana 3pm con Carlos") | ✅ funciona |
| Confirm/reschedule/no-show/completed | ✅ funciona |
| 🟡 Advisor / Vehicle providers | Solo demo (constants). Para tenants reales, raise 501 en `_deps.py:110-127`. |
| 🟡 WhatsApp actions desde la cita | `MessageActionProvider` también es 501 para no-demo. |
| 🟡 Botones NYI | 5 en `AppointmentsPage.tsx:392-846` (detail view, import, filtros avanzados). |
| 🟢 Sin reminder scheduler real | La cita persiste pero no auto-envía recordatorios — sólo si workflow está configurado. |

**Sugerencias:**
- Tu tenant no es demo: vas a topar con 501 al crear citas si no hay advisor/vehicle providers reales. **Más urgente**: implementar `DBAdvisorProvider`/`DBVehicleProvider` que lean de tablas reales.

---

### 7. Conocimiento (Knowledge Base)

| Estado | Notas |
|---|---|
| ✅ CRUD legacy (FAQs, catalog, documentos) | Funciona contra la DB |
| ✅ Upload documento | Funciona, encola `index_document` |
| 🔴 Si arq worker está caído, documento queda en `status="error"` y nunca se indexa | `knowledge_routes.py:511-516` |
| ✅ `/test` query | Usa RAG real (pgvector cosine), fallback ILIKE si no hay embedding |
| 🔴 **Command center hardcoded** | `_kb/command_center.py:704-901` devuelve `HEALTH`, `RISKS`, `KNOWLEDGE_ITEMS`, `UNANSWERED`, `FUNNEL`, `CARDS`, `CONFLICTS`, `AUDIT_LOGS` — todos listas en código, no de DB. |
| 🔴 `POST /knowledge/simulate` | 501 para no-demo |

**Sugerencias:**
- Para `dele.zored` (no demo), el **KB command center** te va a mostrar listas mock — son las únicas que tiene hoy. Esto es lo más urgente de reparar.
- Implementar reads reales: `health` desde `kb_health_snapshots`, `unanswered` desde `kb_unanswered_questions`, `conflicts` desde `kb_conflicts` (las tablas existen pero los endpoints no las usan).

---

### 8. Analítica

| Estado | Notas |
|---|---|
| ✅ Endpoints funnel/cost/volume/handoff existen | Usan datos reales de conversations/handoffs/turn_traces |
| 🟡 Sin tests con rangos largos | Riesgo de scaffolding |
| 🟡 `AnalyticsDashboard.tsx` sin tests |

**Sugerencias:**
- Con tenant nuevo verás 0 en todo. Probar después de mandar algunos mensajes.

---

### 9. Debug de turnos (Turn Traces)

| Estado | Notas |
|---|---|
| ✅ Lista + inspector con narrativa | Funciona (sprint anterior) |
| ✅ Inbound preview + FlowModeBadge | Funciona |
| 🟡 `/turn-traces` sin `?conversation_id=` muestra placeholder | T56 deferido. Para llegar al inspector, copia un UUID de conversación. |
| 🟡 `TurnTraceSections` sin test |

---

### 10. Workflows

| Flujo | Estado |
|---|---|
| Crear + editar workflow | ✅ persiste, valida triggers |
| 🔴 **Simulate devuelve resultados fabricados** | `workflows_routes.py:1271-1311` — hardcoded activated_nodes y generated_response. No ejecuta el grafo. |
| 🔴 **Triggers son cron-based, no inline** | `workflow_jobs.py:38-98` — poll cada 10s en segundos {5,15,25,35,45,55}. Inbound entre polls puede no disparar. |
| 🟡 6 botones NYI | Pause controls, saved views, import, en `WorkflowsPage.tsx:524-680` |
| 🟡 Versiones `v12/v13` dummy | Tracking de versiones es metadata, no historia real |
| 🟡 `WorkflowsPage.tsx`, `WorkflowEditor.tsx` sin tests |

**Sugerencias:**
- Implementar `evaluate_event` inline en el runner igual que ya está cableado para `message_received` (existe pero solo en meta_routes). Cuando llegue un evento, evaluar workflows ahí.
- Implementar simulate real: ejecutar nodos sandbox-style sin side effects de DB.

---

### 11. Config. Bandeja (Inbox-settings)

| Estado | Notas |
|---|---|
| ✅ Persiste config tenant-wide | Funciona |
| 🟡 7 secciones sin tests (solo `types.test.ts` existe) |
| 🟡 No verificado e2e que el inbox real consume todas las opciones | Por ejemplo: ¿filter chips se aplican en `ConversationList`? |

---

### 12. Agentes IA

| Flujo | Estado |
|---|---|
| Crear + editar + publish + rollback | ✅ persiste con audit |
| Decision map / scenarios / stress-test | 🟡 Endpoints devuelven snapshots sintéticos, no ejecuciones reales |
| Asignar agente a conversación | 🔴 Solo guarda `assigned_agent_id`, el runner **no lo lee** para cambiar comportamiento |
| 🟡 3 botones NYI | open docs, upload, view failures en `AgentsPage.tsx` |
| 🟡 `AgentsPage.tsx` sin tests |

**Sugerencias:**
- Cablear el agente asignado al runner: el composer debería leer `agent.tone`, `agent.style`, `agent.guardrails` antes de generar la respuesta del próximo turno.

---

### 13. Configuración (`/config`)

| Estado | Notas |
|---|---|
| ✅ WhatsApp Business API (Meta Cloud) | Card funciona con status real |
| ✅ **Baileys QR** (sprint reciente) | Card nueva con 5 estados, polling, persistencia |
| ✅ Pipeline editor / Brand facts / Tone | Funcionan |
| 🟡 5 componentes de `config/` sin tests |

---

### 14. Usuarios

| Estado | Notas |
|---|---|
| ✅ CRUD usuarios tenant-scoped | Funciona con RBAC matrix |
| 🟡 Sin reset/forgot-password | Punch list documentado |
| 🟡 `UsersPage.tsx` sin tests |

---

### 15. Auditoría

| Estado | Notas |
|---|---|
| ✅ Recibe eventos de la mayoría de endpoints state-changing | `emit_admin_event` cableado |
| 🟡 Acciones NYI no generan audit (no llegan a backend) |

---

### 16. Exportar

| Estado | Notas |
|---|---|
| ✅ CSV de conversaciones y mensajes | Funciona |
| 🔴 **Cero tests** | Único módulo `api/` sin cobertura |

---

## Flujos transversales

### Baileys QR (sprint reciente)
- ✅ Sidecar Node levanta, endpoints registrados.
- ✅ Routing outbound Meta vs Baileys según `tenant_baileys_config.prefer_over_meta`.
- 🟡 **Inbound NO ejecuta el runner** — el operador ve el mensaje pero el bot no responde por Baileys. Phase 2 declarado.
- 🟡 Sin multimedia, sin campañas masivas (declarado Phase 2).

### AI Field Extraction (sprint reciente)
- ✅ Lógica `decide_action` correcta, 22 tests verdes.
- ✅ Sugerencias persisten en `field_suggestions`, panel UI conectado.
- 🟡 Sin push realtime — el `FieldSuggestionsPanel` aparece después del polling de 60s.

### NLU / Composer
- ✅ Funcionan en happy path con `RUN_LIVE_LLM_TESTS=1` + OpenAI key.
- 🟡 Cinco suites live están skipped sin la env var.

### Outbound queue (Meta)
- ✅ Worker arq con retries, circuit breaker, idempotency.
- 🟡 Requiere Redis + arq corriendo.

---

## Cobertura de tests — números honestos

| Capa | Estado |
|---|---|
| Backend | 24 de 25 routers con tests. **Falta:** `exports_routes.py`. |
| Frontend pages-level | **17 de 22 páginas SIN tests** (Agents, Analytics, Appointments, Customers, Dashboard, Handoffs, Knowledge, Pipeline, Users, Workflows, etc.). |
| Componentes de bajo nivel | Mejor cobertura: shadcn UI primitives + nuevos componentes del sprint (FieldSuggestionsPanel, BaileysCard, EditableDetailRow, etc. están testeados). |
| Test E2E (Playwright) | **Deferred** todo el sprint. Sólo 1-2 smokes existían. |

---

## Plan de reparación priorizado

### Sprint A — "Lo que sorprende al usuario" (🔴 alta)
1. Implementar **Workflow simulate real** (`workflows_routes.py:1271`).
2. Cablear **inline workflow trigger** desde el runner cuando llega evento.
3. **Invalidar cache de pipeline** al hacer PUT — al menos un `Redis publish` que el runner escuche.
4. **Reemplazar hardcoded en `_kb/command_center.py`** por reads contra `kb_*` tablas reales.
5. **Implementar `KnowledgeSimulator` real** (`/knowledge/simulate` 501).

### Sprint B — "Funcionalidad incompleta visible" (🟡 media)
6. Wire **agente asignado → composer** del runner.
7. Implementar **DBAdvisorProvider** + **DBVehicleProvider** para reemplazar 501 en non-demo.
8. **Cablear Baileys inbound al runner** (Phase 2 declarado).
9. Convertir **6 NYIButtons de Workflows** en acciones reales o esconderlos por rol.
10. Convertir **5 NYIButtons de Appointments** + dos `DemoBadge` (gate por `is_demo`).

### Sprint C — "Deuda de tests" (🟡 media)
11. Agregar tests a `exports_routes.py`.
12. Tests render-básicos a las 17 páginas frontend sin tests.
13. Playwright smoke para 7 rutas principales (Conversations, Handoffs, Customers, Appointments, Knowledge, Agents, Workflows).

### Sprint D — "Pulido" (🟢 baja)
14. Forgot password flow (con email-out vía nuevo provider).
15. Cross-conversation `/turn-traces` explorer (T56).
16. Pipeline kanban "load more".
17. Reminder scheduler real para appointments.
18. Multimedia + campañas Baileys (Phase 2 ya declarado).

---

## Tu usuario de prueba

Creado un workspace fresco, sin datos demo:

| Campo | Valor |
|---|---|
| Email | `dele.zored@hotmail.com` |
| Password | `dinamo123` |
| Rol | `superadmin` (acceso a todo incluyendo `/audit-log`) |
| Tenant ID | `141ba992-094c-49bd-bd8b-6d9652036913` |
| User ID | `dd857eae-7ec8-4860-9e86-10dfd744e25d` |
| Tenant name | `Zored QA Workspace` |
| `is_demo` | `false` (NO auto-seed de 50 customers, 6 agentes, 6 workflows mock) |

Script para recrear / resetear password: `core/scripts/seed_zored_user.py`.

**Lo que vas a ver con este usuario:**
- Dashboard: KPIs en 0.
- Inbox: vacío.
- Pipeline: vacío.
- Knowledge: **lista mock hardcoded** (problema #4 de la lista).
- Agents: **vacío** (no auto-seed) — pero el endpoint POST funcionará.
- Workflows: **vacío** — POST funcionará.
- Appointments: vacío. Crear cita: probablemente 501 por `advisor/vehicle provider` (problema #7).
- Citas + Handoffs + Clientes: vacíos.

**Recomendación de orden para probar:**
1. Login → ver sidebar / dashboard vacío.
2. Crear un usuario operator desde `/users`.
3. Crear una FAQ + un catalog item en `/knowledge`.
4. Crear un agent simple.
5. Crear un workflow con trigger `message_received` y action `send_message`.
6. Si tienes Meta configurado o Baileys conectado: mandarte un mensaje al número y ver si el bot responde.
